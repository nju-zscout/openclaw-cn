# Openclaw 中文版 Docker 环境配置示例
# 使用方法：
#   1. 复制此文件为 .env
#   2. 根据需要修改配置
#   3. docker compose up -d

# Docker 镜像设置
# 本地开发：openclaw-cn:local
# 公开镜像：jiulingyun803/openclaw-cn:latest
OPENCLAW_IMAGE=jiulingyun803/openclaw-cn:latest

# 数据目录（相对于 docker-compose.yml 所在目录）
OPENCLAW_CONFIG_DIR=./data/.openclaw
OPENCLAW_WORKSPACE_DIR=./data/clawd

# 网关配置
OPENCLAW_GATEWAY_PORT=18789
OPENCLAW_BRIDGE_PORT=18790
OPENCLAW_GATEWAY_BIND=lan
OPENCLAW_GATEWAY_TOKEN=your-secure-token-here

# 额外配置（可选）
OPENCLAW_EXTRA_MOUNTS=
OPENCLAW_HOME_VOLUME=
OPENCLAW_DOCKER_APT_PACKAGES=

# Claude 集成（可选）
# 仅在使用 Claude AI 作为网关后端时需要
CLAUDE_AI_SESSION_KEY=
CLAUDE_WEB_SESSION_KEY=
CLAUDE_WEB_COOKIE=
